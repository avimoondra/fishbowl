- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."players"("id" serial NOT NULL, "uuid" uuid, "game_id"
      integer, "username" text, PRIMARY KEY ("id") , FOREIGN KEY ("game_id") REFERENCES
      "public"."games"("id") ON UPDATE cascade ON DELETE cascade);
  type: run_sql
- args:
    name: players
    schema: public
  type: add_existing_table_or_view
