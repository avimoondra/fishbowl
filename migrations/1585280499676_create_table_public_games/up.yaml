- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."games"("id" serial NOT NULL, "join_code" text,
      "starting_letter" text, "time_limit_per_turn" integer,
      "num_entries_per_player" integer, PRIMARY KEY ("id") , UNIQUE ("join_code"));
  type: run_sql
- args:
    name: games
    schema: public
  type: add_existing_table_or_view
