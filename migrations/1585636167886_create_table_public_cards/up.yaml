- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."cards"("id" serial NOT NULL, "game_id" integer NOT
      NULL, "player_id" integer NOT NULL, "word" text NOT NULL, "created_at" timestamp
      NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("game_id") REFERENCES
      "public"."games"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("player_id")
      REFERENCES "public"."players"("id") ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: cards
    schema: public
  type: add_existing_table_or_view
