query GameStats {
  turn_scorings {
    id
    started_at
    ended_at
    status
    score
    card {
      id
      player_id
      word
    }
    turn {
      id
      player_id
      review_started_at
    }
  }
}
